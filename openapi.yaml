# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: EmailService API
    version: 0.0.1
paths:
    /v1:sendEmail:
        post:
            tags:
                - EmailService
            operationId: EmailService_SendEmail
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/velony.email.api.v1.SendEmailRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/velony.email.api.v1.SendEmailResponse'
components:
    schemas:
        velony.email.api.v1.Email:
            required:
                - from
                - to
                - subject
            type: object
            properties:
                from:
                    $ref: '#/components/schemas/velony.email.api.v1.EmailAddress'
                to:
                    type: array
                    items:
                        $ref: '#/components/schemas/velony.email.api.v1.EmailAddress'
                cc:
                    type: array
                    items:
                        $ref: '#/components/schemas/velony.email.api.v1.EmailAddress'
                bcc:
                    type: array
                    items:
                        $ref: '#/components/schemas/velony.email.api.v1.EmailAddress'
                subject:
                    type: string
                text:
                    type: string
                html:
                    type: string
        velony.email.api.v1.EmailAddress:
            required:
                - address
            type: object
            properties:
                address:
                    type: string
                name:
                    type: string
        velony.email.api.v1.SendEmailRequest:
            required:
                - email
            type: object
            properties:
                email:
                    $ref: '#/components/schemas/velony.email.api.v1.Email'
                idempotencyKey:
                    type: string
        velony.email.api.v1.SendEmailResponse:
            type: object
            properties:
                messageId:
                    type: string
tags:
    - name: EmailService
